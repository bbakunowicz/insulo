import {menuTypes} from 'insulo-menu';
import type {MenuConfigProviderInitValues} from 'insulo-menu';

const init:MenuConfigProviderInitValues = { 
  variant: menuTypes.TEMPORARY,
  width: "20em",
  anchor: menuTypes.MENU_LEFT,
}

export default init;